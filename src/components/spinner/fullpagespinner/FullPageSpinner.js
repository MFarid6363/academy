import React from 'react';
import './FullPageSpinner.css'

const FullPageSpinner = () => {
    return (
        <div className="FullPageSpinner">Loading...</div>
    );
};

export default FullPageSpinner;